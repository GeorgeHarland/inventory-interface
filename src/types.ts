export type ItemData = {
  title: string;
  icon: string;
  description: string;
  sellPrice: number;
  maxStackSize: number;
}

export type ItemType = 'Apple' | 'Book' | 'Flask' | 'Herbs' | 'Sword';
